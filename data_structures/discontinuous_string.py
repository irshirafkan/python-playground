# Discontinuous String Exercise
#
# Write a code that first converts the following string to lowercase,
# and then outputs the string as a list of separate words using a string method.


text = input()
result = text.lower().split()
print(result)